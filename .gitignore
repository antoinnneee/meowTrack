# Base de REGISTRE locale (par machine) + artefacts WAL SQLite.
meowtrack.db
meowtrack.db-wal
meowtrack.db-shm

# Bases « tracker.db » PAR dépôt (données de tracking, cloisonnées par dépôt).
# Phase 1 : simple dossier local. Phase 2 : worktrees de la branche orphan « tracking »
# (versionnés DANS chaque dépôt, donc hors du dépôt de CODE meowtrack).
.trackers/

# Dépendances Node.
node_modules/

# Données de l'outil Claude Code (worktrees, état de session) — jamais versionné…
.claude/*
# …SAUF les skills meowtrack (NODE-357), eux VERSIONNÉS et déployés avec le dépôt.
!.claude/skills/

# Secrets de déploiement / config locale.
.deployEnv
deployEnv
.env

# Clones des repos gérés par le service (multi-repos : .repos/<slug>/ ; legacy : .repo-clone/).
.repos/
env
# Clone du repo géré par le service (MEOWTRACK_REPO_URL sans MEOWTRACK_REPO).
.repo-clone/
.meowtrack/
